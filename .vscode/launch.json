{
  "configurations": [
    {
      "name": "Start Vite",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev", "--", "--host"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
